// Temporary stub — full implementation in Task 4
export default function SafetyTicker({ destinationId, center, zoom }) {
  return (
    <div style={{ height: 32, display: 'flex', alignItems: 'center', padding: '0 12px', fontFamily: "'JetBrains Mono', monospace", fontSize: 10, color: '#8A8680', background: 'var(--surface-raised)', borderBottom: '1px solid var(--border)' }}>
      ● Safety Pulse loading…
    </div>
  );
}
