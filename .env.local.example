# VenturePath environment variables (copy to .env.local)
VITE_ORS_API_KEY=                 # OpenRouteService — required for routing
VITE_TOLLGURU_KEY=                # TollGuru — required for toll/route-variant data (Phase 1)
                                  # Get a free 1k/mo key at https://tollguru.com
